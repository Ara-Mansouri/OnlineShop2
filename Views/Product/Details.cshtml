@model OnlineShop.Models.Product

<div class="container">
    <h1>@Model.Name</h1>
    <img src="@Model.ImageUrl" alt="@Model.Name" class="img-fluid mb-4" />
    <p>@Model.Description</p>
    <p class="text-primary">@Model.Price.ToString()تومان </p>
    <a href="/Product/ProductsByCategory?categoryId=@Model.CategoryId" class="btn btn-secondary">Back to Products</a>
</div>
